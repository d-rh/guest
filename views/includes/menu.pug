block menu
  - let user;
  - user=username;
  .header
    a(class="header__logo" href="/")
      h1 Rockwell Guestbook
    unless user
      div(class="header__button")
        a(class="header__button_login" href="login")      Log In
    if user 
      a(class="header__button" href="feed") Check In
      form(method="post" action="/logout")
        input(type="hidden")
        button(type="submit") Log Out